import { UniversalRulesService } from './universal-rules.service';

describe('UniversalRulesService', () => {
  it('should be defined', () => {
    // SSS-Tier Automated Scaffold
    expect(true).toBe(true);
  });
});