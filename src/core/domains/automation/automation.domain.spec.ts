import { Automation.domain } from './automation.domain';

describe('Automation.domain', () => {
  it('should be defined', () => {
    // SSS-Tier Automated Scaffold
    expect(true).toBe(true);
  });
});