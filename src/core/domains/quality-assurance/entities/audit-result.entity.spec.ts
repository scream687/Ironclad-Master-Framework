import { AuditResult.entity } from './audit-result.entity';

describe('AuditResult.entity', () => {
  it('should be defined', () => {
    // SSS-Tier Automated Scaffold
    expect(true).toBe(true);
  });
});