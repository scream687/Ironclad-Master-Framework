# GEMINI.md — Ironclad Master Framework (Universal Synthesis)

This repository is governed by the **Ironclad Master Framework**, a high-performance shell synthesized from the world's most elite AI engineering workflows (ECC, Karpathy, Matt Pocock, Superpowers, Claude Plugins, Agent-Browser, Understand-Anything).

## The God-Tier Operational Loop (MANDATORY)

Every task MUST progress through these five phases. Confirm the active phase in your response.

1. **Understand**: Use `Understand-Anything` to map architectural dependencies and data flows.
2. **Plan**: Use `superpowers:writing-plans` to create a strategic SPARC Specification.
3. **Delegate**: Use `ECC` Swarm mechanics to spawn specialized parallel agents.
4. **Implement**: Apply `claude-plugins:frontend-design` standards and `karpathy-guidelines`.
5. **Verify**: Use `agent-browser` for visual QA and `verification-quality` for threshold checks.

## Core Mandates

### 1. The Karpathy Mandate: "Simplicity First"
- **Thinking**: State assumptions explicitly. Push back on complexity.
- **Action**: Surgical changes only. If 200 lines can be 50, rewrite it. No speculative features.

### 2. The Pocock Mandate: "Deep Architecture"
- **Shared Language**: Reference `CONTEXT.md` for ubiquitous jargon.
- **Feedback Loops**: Red-Green-Refactor is mandatory. Use the `tdd` skill.

### 3. The ECC Mandate: "Enterprise Governance"
- **Orchestration**: Consult `SKILL_ROUTER.md` before every response.
- **Learning**: Use `make upgrade` to distill patterns into framework intelligence.

## Gemini CLI Platform Bindings
- **Invoke Skills**: `activate_skill("skill-name")`
- **Spawn Agents**: Use `@generalist` with SPARC prompt templates.
- **Task Tracking**: `tracker_create_task` for rich management.

---

# ⚠️ IRONCLAD RULES — ENFORCED EVERY SESSION, EVERY MESSAGE

These rules CANNOT be disabled. They apply even if the user says nothing.
They persist across sessions. They survive restarts.

## Rule 1 — Skills Before Code
Before writing ANY code:
1. Session start → invoke `karpathy-guidelines` (behavioral foundation)
2. Check docs/SKILLS.md → invoke the matching skill
3. Minimum required: brainstorming (new features), ui-ux-pro-max (UI), verification-before-completion (claiming done)

## Rule 2 — Plugins Before Tools
Before ANY operation that a plugin can handle:
1. Check docs/PLUGINS.md → use the MCP tool or plugin instead
2. Supabase → MCP, not raw queries; Vercel → MCP, not terminal

## Rule 3 — Agents Before Solo Work
Before ANY task with 2+ independent operations:
1. Check docs/ORCHESTRATION.md → spawn parallel agents in ONE message
2. Minimum for any feature: code-architect (design) → [implementation] → code-reviewer (review)

## Rule 4 — MCPs Before Manual Code
Before ANY operation listed in docs/MCP.md:
1. Use the MCP tool
2. If MCP fails → then write code manually
3. NEVER write code that duplicates what an MCP can do

## Rule 5 — UI/UX Intelligence Required
Before ANY public-facing UI component or page:
1. Activate `ui-ux-pro-max` skill — visual system
2. Activate `design-motion-principles` — motion
3. Apply project design system tokens

## Rule 6 — Memory Persistence
After every phase or significant decision, persist to memory store:
- namespace, key (phase/feature-date), value (decision, why, outcome)

## Rule 7 — Verification Before Done
NEVER claim a feature is done without:
1. Activate `verification-quality` skill
2. Running the project build command → exit 0
3. Verifying the route/feature renders correctly

## Rule 8 — Mobile First
Every UI change must be verified at 375px (mobile) BEFORE claiming done.
Min text: 14px. Primary CTA always visible without scrolling.

## Rule 9 — Understand-Anything Reference
Repo: https://github.com/Lum1104/Understand-Anything.git
Use when deep codebase understanding is needed as an alternative to graphify.
For visual/multimodal understanding of complex code patterns.

## Rule 10 — Karpathy Behavioral Rules
- No speculative code. No extra features. Minimum that solves the problem.
- State assumptions explicitly. Push back when warranted.
- Touch only what you must. Match existing style.

---

## REFERENCE FILES

| File | When to read |
|---|---|
| `SKILL_ROUTER.md` | Before every response — governance |
| `docs/ORCHESTRATION.md` | Before any multi-task work |
| `CONTEXT.md` | For shared language (Pocock) |

---
*Last Updated: June 11, 2026 (Phase 8: Production-Proven Rules Sync)*
---
*Stay Ironclad.*
