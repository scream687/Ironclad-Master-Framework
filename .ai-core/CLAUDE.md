# graphify
- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# UI/UX Pro Max — Design Intelligence System
- **ui-ux-pro-max** (`scream687/ui-ux-pro-max`) — 67 styles, 96 palettes, 57 font pairings, 99 UX guidelines
- **CRITICAL:** Primary authority for UI/UX work. Use for design systems and industry-specific intelligence.
- Before ANY UI work, run: `python3 skills/ui-ux-pro-max/scripts/search.py "<query>" --design-system`
- Default stack: `html-tailwind`
- Invoke via `Skill({ skill: "ui-ux-pro-max" })`

# Impeccable — Anti-Slop Frontend Design
- **impeccable** (`pbakaus/impeccable`) — 23 design commands + 27 deterministic anti-pattern rules
- Before ANY frontend UI work, run impeccable audit to catch generic/AI-slop patterns
- Key commands: `/polish`, `/audit`, `/critique`, `/typeset`
- Invoke via `Skill({ skill: "impeccable" })` or specific commands like `Skill({ skill: "impeccable", args: "audit" })`

# claude-mem — Persistent Session Memory
- **claude-mem** (`thedotmack/claude-mem`) — captures session context, compresses it, injects into future sessions
- Runs automatically via lifecycle hooks (SessionStart, SessionEnd)
- Local SQLite + Chroma vector search — everything stays on-device
- Complements the built-in auto-memory system with richer session continuity

# Taste — 5-Dimension Engineering Taste
- **taste** (`VOIDXAI/taste`) — evaluates code, architecture, product, design, and communication quality
- Use when the question is "is this good?" not "does this work?"
- Modes: Quick Judgment, Deep Review, Guide, Compare
- Invoke via `Skill({ skill: "taste" })` before finalizing any significant code or design decision

# Design Motion Principles
- **design-motion-principles** (`kylezantos/design-motion-principles`) — purposeful motion & animation audit
- Two modes: build interactive components with motion, or audit existing animations
- Context-aware guidance from Emil Kowalski, Jakub Krehel, Jhey Tompkins
- Invoke via `Skill({ skill: "design-motion-principles" })` before any animation/transition work

# Browser Preference — Vercel Agent Browser (NOT Playwright)
- **agent-browser** (`vercel-labs/agent-browser`) — browser automation CLI for AI agents
- For ALL browser automation, screenshots, testing, and debugging: use agent-browser
- **NEVER** use Playwright MCP tools (`mcp__plugin_playwright_playwright__*`)
- Key workflow: `agent-browser open <url>` → `agent-browser snapshot -i` → interact with refs
- Includes live dashboard with viewport + command feed, AI chat via Vercel AI Gateway
- Invoke via `Skill({ skill: "agent-browser" })` for browser tasks

# Ruflo Integration (auto-generated by ruflo init)
When working on multi-file tasks or complex features, use ToolSearch to find and invoke ruflo MCP tools.
Key tools: memory_store, memory_search, hooks_route, swarm_init, agent_spawn.
Check system-reminder tags for [INTELLIGENCE] pattern suggestions before starting work.
